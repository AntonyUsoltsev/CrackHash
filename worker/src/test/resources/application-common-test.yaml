spring:
  application:
    name: worker
  jackson:
    mapper:
      accept-case-insensitive-enums: true
    constructor-detector: use_properties_based

server:
  port: 8902

app:
  manager:
    baseUrl: ${APP_MANAGER_BASEURL}
